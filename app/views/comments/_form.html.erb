<%= form_with model: [ post, comment ] do |form| %>
  <p>
    <%= form.text_area :body %>
    <% comment.errors[:body].each do |message| %>
      <div><%= message %></div>
    <% end %>
  </p>
  
  <p>
    <%= form.submit %>
  </p>
<% end %>
